export class User {
  email: string;
  priority: boolean;
  subject: string;
  content: string;
  value: string;
  // maybe i do not need the constructor
  // constructor() {
  //   this.value = Math.round(Math.random() * 1000);
  //   console.log(this.value);
  // }
}
